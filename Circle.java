class Circle {
    /*@ specification Circle {
            double r, d, S, p;
            const double pi = Math.PI;
            S = pi * r ^ 2;
            p = pi * d;
            d = 2 * r;
        }
@*/
}
